services:
  - type: web
    name: youtube-downloader
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --keep-alive 5 app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    autoDeploy: true
